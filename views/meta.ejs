<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>META page</title>
  <script>
    setTimeout(() => {
      const gebDomain = 'http://localhost:3000'
      fetch(gebDomain + '/callback', {credentials:"include", body: new URLSearchParams({metaData: 'metaData'}), method:'POST'})
      location.href = gebDomain
    }, 3000);
  </script>
</head>
<body>
  <h1>This is Family service page, we are auto post the {metaData: 'metaData'} in 3 second...</h1>
  FYI:
  <code>
    setTimeout(() => {
      const gebDomain = 'http://localhost:3000'
      fetch(gebDomain + '/callback', {credentials:"include", body: new URLSearchParams({metaData: 'metaData'}), method:'POST'})
      location.href = gebDomain
    }, 3000);
  </code>
</body>
</html>